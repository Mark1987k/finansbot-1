spring:
  application:
    name: finansbot
  main:
    banner-mode: "off"
    web-application-type: none

server:
  port: 8080

# Twitter API credentials from environment variables
# Scheduler configuration
app:
  scheduler:
    enabled: true
    cron: "0 0,30 * * * *"
